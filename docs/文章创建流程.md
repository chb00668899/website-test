文章创建流程
1. 用户提交表单
在 /src/app/admin/posts/new/page.tsx 中，用户填写标题、slug、内容等信息并提交。

2. 前端调用 API
page.tsx:67-74 - 通过 fetch('/api/posts', { method: 'POST' }) 发送请求，携带 cookie 认证信息。

3. API 路由处理
src/app/api/posts/route.ts:103-150


export async function POST(request: Request) {
  // 1. 从 cookie 验证用户身份
  const currentUser = await getCurrentUser(request);
  
  // 2. 获取请求体数据
  const body = await request.json();
  
  // 3. 生成唯一 slug（自动处理重复）
  const finalSlug = generateUniqueSlug(title, existingSlugs);
  
  // 4. 调用服务层插入数据库
  const post = await AdminPostService.createPost({
    title,
    slug: finalSlug,      // API 会自动生成唯一 slug
    content,
    category_id,
    tags,
    status: status || 'draft',
    author_id: currentUser.id,
  });
}
4. 数据库插入
src/lib/supabaseAdmin.ts:146-159


async createPost(postData: Omit<Post, 'id' | 'created_at' | 'updated_at' | 'view_count'>) {
  const { data, error } = await this.client
    .from('posts')
    .insert([postData])   // 直接插入数据库
    .select()
    .single();
}
数据流总结：

前端表单 → /api/posts POST → getCurrentUser 认证 → generateUniqueSlug 生成 slug 
→ AdminPostService.createPost → supabaseAdmin.client.from('posts').insert() → Supabase